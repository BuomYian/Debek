# components/features/

Per-module components (`patients/`, `appointments/`, `billing/`, …),
built alongside each module starting Phase 4. `components/ui/` stays
reserved for unmodified shadcn/ui primitives — module-specific
composition (forms, tables, calendars, print views) lives here instead.
