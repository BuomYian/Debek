import { ModulePlaceholder } from "@/components/module-placeholder";

export default function DashboardPage() {
  return (
    <ModulePlaceholder
      title="Dashboard"
      description="Role-aware landing dashboard with today's snapshot."
      phase="Phase 3 (auth-aware) and Phase 11 (charts)"
    />
  );
}
